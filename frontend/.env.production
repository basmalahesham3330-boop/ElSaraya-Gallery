# Production Environment Configuration
# Leave empty to use relative path when frontend and backend are on same domain
# Or set to your production API URL (e.g., https://api.yourdomain.com/api/v1)
VITE_API_URL=
