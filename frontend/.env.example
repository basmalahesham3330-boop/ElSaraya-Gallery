# API Configuration
# Backend API URL (e.g., http://localhost:8000/api/v1 for local development)
# Leave empty to use relative path (/api/v1) - useful when frontend and backend share same domain
VITE_API_URL=http://localhost:8000/api/v1
